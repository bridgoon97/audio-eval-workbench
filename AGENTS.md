# 项目协作要求

- 使用中文沟通，界面、注释和项目文档使用中文；代码标识符遵循语言惯例。
- 这是独立音频评测应用，不修改任何算法生产仓库或训练分支。
- 仓库公开。不得提交真实录音、模型、数据库、凭据、内网地址或带本机绝对路径的报告。演示素材只能由公开生成器在运行时生成。
- 默认保留音频相对电平；不得静默归一化、对齐、拉伸或替换已发布资产。
- 隐藏版本身份与其他人的未公开意见必须由后端权限保护；检查媒体元数据、直接 URL 和导出，不只隐藏页面文本。
- 变更播放引擎时必须验证真实渲染与非恒等变异；变更协作逻辑时验证越权、任务冻结、并发和恢复。
- 运行与变更相关的测试以及前端构建；不得将 CPU/CI 通过宣称为实体设备听音或真实算法效果通过。
- 不添加未经需求支持的云服务、遥测、外部字体或 CDN。
- Python 项目统一使用 uv：依赖写入 pyproject.toml 并提交 uv.lock；运行使用 uv run --locked，不另维护 requirements.txt。
